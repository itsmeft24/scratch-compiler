#include "ResourceAccessor.hpp"

namespace scratch {
	void ResourceAccessor::initialize()
	{
		// "project.sb3"
		// load zip, parse into std::map.
		// scratch loads all files so issok
	}
	Resource ResourceAccessor::acquire(const std::string& str)
	{
		return Resource();
	}
};